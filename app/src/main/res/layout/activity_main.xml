<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Latency Tracker"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Network Type Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardNetwork"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvNetworkType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="Network Type: Loading..."
            android:textSize="16sp" />
    </androidx.cardview.widget.CardView>

    <!-- Download Speed Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardDownload"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/cardNetwork"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvDownloadSpeed"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="Download Speed: -- Mbps"
            android:textSize="16sp" />
    </androidx.cardview.widget.CardView>

    <!-- DNS Latency Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardDns"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/cardDownload"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvDnsLatency"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="DNS Latency: -- ms"
            android:textSize="16sp" />
    </androidx.cardview.widget.CardView>

    <!-- Run DNS Benchmark -->
    <Button
        android:id="@+id/btnDnsBenchmark"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Run DNS Benchmark"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/cardDns"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Shows the fastest DNS after the run -->
    <TextView
        android:id="@+id/tvDnsFastest"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Fastest DNS: —"
        android:textStyle="bold"
        android:paddingTop="6dp"
        app:layout_constraintTop_toBottomOf="@id/btnDnsBenchmark"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Per-App Data Usage List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvUsageStats"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvDnsFastest"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:listitem="@layout/item_app_usage" />

</androidx.constraintlayout.widget.ConstraintLayout>
